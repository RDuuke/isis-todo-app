import { Routes } from '@angular/router';
import { TodoListPage } from './features/todo/pages/todo-list/todo-list.page';

export const routes: Routes = [
    {
        path: '',
        component: TodoListPage
    }
];
